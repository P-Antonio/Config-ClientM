server:
   port: 8094
spring:
  application:
    name: mcsv-ForoUsuarios
  datasource:
    driver-class-name: com.posgresql.Driver
    url: jdbc:postgresql://localhost:5432/Foro
    username: root
    password: root1234
  jpa:
     hibernate:
       ddl-auto: create
     database: postgresql
     database-platform: org.hibernate.dialect.PostgreSQLDialect
      
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka